
public class Java_Constructors {
	
	/*
	 * Constructor : A constructor is a special method that is used to initialize class variable at the time of object creation.
	 * 
	 * Each time an object is created using the new() keyword, a constructor is called. A constructor can be created by the programmer.
	 * If the developer does not create any constructor then, Java provides a default constructor.
	 * 
	 * Constructor have the same name as that of the class and does not have a return type.
	 * 
	 * Constructor are of two types: 
	 * 1. Parameterless constructor.
	 * 2. Parameterized constructor.
	 * 
	 */

}
