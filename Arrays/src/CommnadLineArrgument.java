
public class CommnadLineArrgument {
	/*
	 * 
	 * instead of args we can use any name.
	 * 
	 * and for setting command line argument we have to set run configuration.
	 * 
	 * 
	 */
	public static void main(String[] manohar) {
		System.out.println("Hello " + manohar[0]);
		System.out.println("Hello " + manohar[1]);
	}
}
